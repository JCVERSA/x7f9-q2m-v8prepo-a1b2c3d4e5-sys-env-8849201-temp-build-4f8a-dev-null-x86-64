name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 350

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ==========================================
      # RESTAURATION DE LA MÉMOIRE (CACHE GITHUB)
      # ==========================================
      - name: Restaurer les données persistantes
        id: cache-rdp
        uses: actions/cache@v4
        with:
          path: C:\RDP_Data
          key: rdp-data-v1-${{ github.run_id }}
          restore-keys: |
            rdp-data-v1-

      - name: Initialiser l'arborescence persistante
        shell: powershell
        run: |
          $dirs = @(
              "C:\RDP_Data",
              "C:\RDP_Data\Projects",
              "C:\RDP_Data\ChromeProfile",
              "C:\RDP_Data\AndroidConfig\.android",
              "C:\RDP_Data\AndroidConfig\.AndroidStudio",
              "C:\RDP_Data\AndroidConfig\.gradle"
          )
          foreach ($dir in $dirs) {
              if (-not (Test-Path $dir)) {
                  New-Item -ItemType Directory -Path $dir -Force
              }
          }

      # ==========================================
      # CONFIGURATION DU SYSTÈME & RDP
      # ==========================================
      - name: Configure Core RDP Settings
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force

          # Sécurisation stricte : RDP autorisé uniquement via l'IP VPN Tailscale
          netsh advfirewall firewall delete rule name="RDP-Tailscale" 2>$null
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389 remoteip=100.64.0.0/10

          Restart-Service -Name TermService -Force

      - name: Create RDP User
        shell: powershell
        run: |
          $securePass = ConvertTo-SecureString "${{ secrets.RDP_PASS }}" -AsPlainText -Force
          if (-not (Get-LocalUser -Name "Neon" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "Neon" -Password $securePass -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member "Neon"
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Neon"
          }

      # ==========================================
      # INSTALLATION DES OUTILS DEV (CORRIGÉE)
      # ==========================================
      - name: Installer les applications de travail
        shell: powershell
        run: |
          $packages = @(
              "Google.Chrome",
              "Microsoft.VisualStudioCode",
              "Git.Git",
              "Oracle.JDK.21",
              "Google.AndroidStudio"
          )
          foreach ($pkg in $packages) {
              Write-Host "Installation de $pkg..."
              # L'argument qui causait le crash a été retiré ici
              winget install --id $pkg --silent --accept-source-agreements --accept-package-agreements
          }

      # ==========================================
      # FORCER LA PERSISTANCE D'ANDROID STUDIO
      # ==========================================
      - name: Rediriger les données Android Studio vers le dossier persistant
        shell: powershell
        run: |
          [Environment]::SetEnvironmentVariable("GRADLE_USER_HOME", "C:\RDP_Data\AndroidConfig\.gradle", "Machine")
          [Environment]::SetEnvironmentVariable("ANDROID_USER_HOME", "C:\RDP_Data\AndroidConfig\.android", "Machine")
          [Environment]::SetEnvironmentVariable("ANDROID_EMULATOR_HOME", "C:\RDP_Data\AndroidConfig\.android", "Machine")
          
          echo "GRADLE_USER_HOME=C:\RDP_Data\AndroidConfig\.gradle" >> $env:GITHUB_ENV
          echo "ANDROID_USER_HOME=C:\RDP_Data\AndroidConfig\.android" >> $env:GITHUB_ENV

      # ==========================================
      # RÉSEAU VPN (TAILSCALE)
      # ==========================================
      - name: Install Tailscale
        shell: powershell
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          $process = Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait -PassThru
          if ($process.ExitCode -ne 0) { exit 1 }
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-${{ github.run_id }}
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 12) {
              Start-Sleep -Seconds 5
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              $retries++
          }
          if (-not $tsIP) { exit 1 }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify RDP Accessibility
        shell: powershell
        run: |
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) { exit 1 }

      # ==========================================
      # MAINTIEN DE LA SESSION & LOGS
      # ==========================================
      - name: Maintain Connection
        shell: powershell
        run: |
          Write-Host "`n===============================================" -ForegroundColor Green
          Write-Host "          CONNEXION RDP PRÊTE                  " -ForegroundColor Green
          Write-Host "===============================================" -ForegroundColor Green
          Write-Host "Adresse IP Tailscale : $env:TAILSCALE_IP" -ForegroundColor Cyan
          Write-Host "Utilisateur          : Neon" -ForegroundColor Cyan
          Write-Host "Dossier Persistant   : C:\RDP_Data" -ForegroundColor Yellow
          Write-Host "-----------------------------------------------"
          Write-Host "Créez vos projets Android dans : C:\RDP_Data\Projects" -ForegroundColor Yellow
          Write-Host "==============================================="`n

          $runtime = 0
          while ($runtime -lt 21000) {
              Write-Host "[$(Get-Date -Format 'HH:mm:ss')] Session RDP active..."
              Start-Sleep -Seconds 300
              $runtime += 300
          }
